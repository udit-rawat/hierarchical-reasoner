# OS
.DS_Store

# Local automation
push-reminder.sh

# Checkpoints & model weights
checkpoints/
hrm_checkpoint.pt


# Notebooks (local scratch)
notebooks/

# Results (generated locally)
results/

# Python cache
src/__pycache__/
tests/__pycache__/
experiments/__pycache__/
src/__init__.py

# -------------------------------------------------------
# PROJECT TODO — local reference, not tracked by git
# -------------------------------------------------------
# CRITICAL
# [x] Add requirements.txt (torch, numpy, matplotlib, pyyaml, tqdm, py-sudoku)
# [x] Fill configs/model_config.yaml and configs/train_config.yaml (currently empty)
# [x] Run experiments/run_hrm_sudoku.py with 1000 samples — validate paper's Sudoku claim
#
# IMPORTANT
# [x] Implement src/dataset_maze.py — grid maze generation, input: layout+start/goal, output: path
# [x] Fix notebooks/sanity_check.ipynb — corrupted JSON, recreate with trajectory viz
#
# POLISH
# [x] Add tests/ with pytest suite (model shapes, dataset gen, checkpoint save/load, metrics)
# [x] Train ACT module — implement Q-learning reward signal, compare fixed vs adaptive depth
# [x] Implement one-step gradient approximation — paper's actual training technique (not BPTT)
# [x] Expand README — setup, usage, results, comparison with paper claims
#
# -------------------------------------------------------
# PHASE 2 — MAKING IT A COMPLETE PAPER REPRODUCTION
# -------------------------------------------------------
#
# CRITICAL
# [ ] Run full 9×9 Sudoku training overnight (1000 samples, 100 epochs) — validate headline claim
# [ ] Joint ACT + HRM training — unfreeze HRM weights during ACT training, close accuracy gap
# [ ] Implement ARC benchmark dataset — paper's biggest claim, format ARC-AGI tasks for HRM
#
# IMPORTANT
# [ ] Run maze experiment end-to-end — train HRM on maze, validate optimal path finding
# [ ] Add joint training script — experiments/train_joint_act.py (ACT + HRM co-trained)
# [ ] Visualize reasoning trajectories — plot H-step activations for Sudoku solutions
# [x] Compare HRM depth vs accuracy — how does num_steps affect Sudoku solve rate?
#
# POLISH
# [ ] Add ARC experiment script — experiments/run_hrm_arc.py
# [ ] Expand pytest suite — add Maze + ACT + gradient tests
# [ ] Add training curves to README — loss plots for Sudoku and Maze
# [ ] Benchmark inference speed — HRM vs RNN on Sudoku, measure ms/sample
# [ ] Write proper theory_notes update — document all experiment findings
